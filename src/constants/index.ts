// Re-export from colors module for backward compatibility
export {
  ELEMENTS,
  DEFAULT_ELEMENT,
  getElement,
  getElementColor,
  getVdwRadius,
  getCovalentRadius,
  CHAIN_COLORS,
  getChainColor,
  type ElementData,
} from '../colors';
