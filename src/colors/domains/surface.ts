/**
 * Default surface colors with theme variants
 */
export const SURFACE_COLORS = {
  dark: '#4488ff',
  light: '#3b82f6',
} as const;

/**
 * Default surface color (used when theme is not available)
 */
export const DEFAULT_SURFACE_COLOR = '#4488ff';
