# New Relic Error Logging (Log API)
# This is a browser ingest key (BROWSER type), intentionally client-side.
# It can only write log data to New Relic — it cannot read, query, or delete anything.
# Store the actual value in Cloudflare Pages > Settings > Environment Variables.
VITE_NEW_RELIC_LICENSE_KEY=
